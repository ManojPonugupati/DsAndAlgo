package arrays;

public class DualElement {
    Integer first_index;
    Integer next_index;

    public DualElement(Integer first_index, Integer next_index) {
        this.first_index = first_index;
        this.next_index = next_index;
    }
}
